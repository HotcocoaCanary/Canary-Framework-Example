## 变更说明 / Description

<!-- 这个 PR 改了哪个示例、为什么 / Which example changed, and why -->

## 检查清单 / Checklist

- [ ] 改动涉及的工程里 `uv run pytest` 通过 / `uv run pytest` passes in each project touched
- [ ] `uvx ruff@0.15.15 check .` 通过 / passes
- [ ] 依赖变化时已更新 `uv.lock` / `uv.lock` updated when dependencies changed
- [ ] README 与代码保持一致 / READMEs match the code
